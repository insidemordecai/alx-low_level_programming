#include "main.h"

/**
 * _isupper - check for uppercase character
 * @c: provided character
 * Return: 1 if c is uppercase, 0 otherwise
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'B')
	{
		return (1);
	}

	return (0);
}
